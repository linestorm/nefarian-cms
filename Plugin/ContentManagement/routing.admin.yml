index:
  path:     /
  defaults: { _controller: ContentType:index }

new:
  path:     /new
  defaults: { _controller: ContentType:new }

manage:
  path:     /manage
  defaults: { _controller: ContentType:index }

edit:
  path:     /{id}/edit
  defaults: { _controller: ContentType:index }

api:
  type:         rest
  prefix:       /api
  resource:     "routing.api.yml"
